[pytest]
# Ensures the repo root (where main.py lives) is always importable in tests,
# regardless of whether pytest is invoked as `pytest` or `python -m pytest`.
# Without this, `import main` in tests/test_app.py can fail with
# ModuleNotFoundError in CI environments that don't add rootdir to sys.path.
pythonpath = .
